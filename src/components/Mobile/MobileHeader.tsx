"use client"
// import { Menu } from "lucide-react"
// import { useSidebar } from "@/components/ui/sidebar"
// import { AppSidebar } from "../app-sidebar"

export function MobileHeader() {
  // const { open, close, isOpen } = useSidebar()

  return (
    <></>
    // <>
    //   <header className="fixed top-0 left-0 right-0 h-[50px] bg-white flex items-center justify-between px-4 shadow-md z-50">
    //     <div className="text-lg font-bold">
    //       <a href="/">Logo</a>
    //     </div>

    //     <button aria-label="Apri menu" onClick={open} className="p-2">
    //       <Menu size={24} />
    //     </button>
    //   </header>

    //   {isOpen && (
    //     <div
    //       className="fixed inset-0 bg-black bg-opacity-40 z-40 flex"
    //       onClick={close}
    //     >
    //       <div
    //         className="w-64 h-full bg-white shadow-lg"
    //         onClick={(e) => e.stopPropagation()}
    //       >
    //         <AppSidebar />
    //       </div>
    //     </div>
    //   )}
    // </>
  )
}
